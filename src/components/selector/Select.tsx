import React from 'react';
import styled from 'styled-components';

const Select: React.FC = () => {
  return (
    <PrioritySeletor>

    </PrioritySeletor>
  )
}

const PrioritySeletor = styled.select`
  width:130px;
  height:48px;
`
export default Select;